<!DOCTYPE html>
<html data-theme="winter">
  <head>
    <title>Higher Eduction Access Prediction System</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="stylesheet" href="/stylesheets/aos.css" />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
  <body>
    <% include ../partial/navbar.ejs %> <% include ../partial/subMenu.ejs %>
    <!-- <div class="aos-all"> -->
    <div class="container mx-auto px-4 py-6">
      <!-- Header -->
      <div class="mb-6">
        <h1 class="text-3xl font-bold text-primary">📘 <%= quiz.title %></h1>
        <p class="text-gray-500">
          An overview of the quiz information and settings
        </p>
      </div>

      <!-- Exam Info Card -->
      <div class="card bg-base-100 shadow-xl">
        <div class="card-body space-y-4">
          <!-- Description -->
          <div>
            <h2 class="text-lg font-semibold">📝 Description</h2>
            <p class="text-gray-700"><%= quiz.description %></p>
          </div>

          <!-- Details Grid -->
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
            <div class="bg-base-200 p-4 rounded">
              <span class="font-semibold">Duration:</span>
              <p><%= quiz.durationMinutes %> minutes</p>
            </div>
            <div class="bg-base-200 p-4 rounded">
              <span class="font-semibold">Marks:</span>
              <p><%= quiz.mark %></p>
            </div>
            <div class="bg-base-200 p-4 rounded">
              <span class="font-semibold">University:</span>
              <div class="mt-1 space-x-2">
                <% for(var i = 0; i < quiz.category.length; i++){%>
                <span class="badge badge-primary my-1"
                  ><%= quiz.category[i] %></span
                >
                <%}%>
              </div>
            </div>
            <div class="bg-base-200 p-4 rounded">
              <span class="font-semibold">Status:</span>
              <p class="text-green-500 font-medium">
                <%= quiz.isPublished ? "Published" : "Draft" %>
              </p>
            </div>
          </div>

          <!-- Action Buttons (optional) -->
          <div class="mt-6 flex flex-wrap gap-2">
            <% if(isDone){%>
            <a href="/user/quiz/result/<%= quiz._id %>" class="btn btn-outline"
              >📄 View result</a
            >
            <%}else{%>
            <a href="/user/quiz/taken/<%= quiz._id %>" class="btn btn-primary"
              >✏️ Take Quiz</a
            >
            <%}%>
          </div>
        </div>
      </div>
    </div>

    <!-- </div> -->
    <% include ../partial/footer.ejs %>
  </body>
  <script src="/javascripts/aos.js"></script>
  <script>
    AOS.init({
      mirror: true,
    });
  </script>
</html>
